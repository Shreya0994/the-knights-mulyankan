import { ReccomendationsProps } from "@/library/types";
import { TestimonialItems } from "./testimonial-items";

const ReccomendationsData: ReccomendationsProps = {
  title: "What they say about us",
  testimonials: TestimonialItems,
};

export { ReccomendationsData };
